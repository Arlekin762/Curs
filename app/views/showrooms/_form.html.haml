= form_for @showroom do |f|
  = error_messages_for @showroom

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :collection_id
    = f.select :collection_id, Collection.all.collect{|x| [x.name, x.id]}
  .actions
    = f.submit 'Сохранить'
